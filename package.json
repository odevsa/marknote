{
  "name": "marknote",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "npx concurrently -n \"frontend,backend\" -c \"cyan.bold,magenta.bold\" \"npm --prefix frontend run dev\" \"cargo run --manifest-path backend/Cargo.toml\"",
    "build": "npm --prefix frontend run build && cargo build --manifest-path backend/Cargo.toml --release"
  }
}
